# stall.one

A minimal, responsive single-page portfolio/landing site.

## Project Summary

- Purpose: Simple personal landing/profile site, optimized for GitHub Pages.
- Tech: Plain HTML and CSS — zero-build, easy to edit.
- Files: `index.html`, `style.css`, `CNAME`
- Deployment: Push to a GitHub repo and enable Pages; use `CNAME` for a custom domain.
- Notes: Replace placeholder content and update `CNAME` before publishing.
